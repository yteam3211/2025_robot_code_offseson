package frc.robot;

public class alerts {
  /*
   * @placeofalert is the place where the alert is coming from
   * this function is used to put alerts in the console
   */
  public static void putalert(String placeofalert) {
    System.out.println("ALERT: at" + placeofalert);
  }
}
